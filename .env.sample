# Rails
# RAILS_ENV=production bundle exec rake secret
SECRET_KEY_BASE='xxxxxx'

# fediverse.observer API key (https://api.fediverse.observer/keygenerator.php).
# Required from datacenter IP ranges; Cloudflare returns 403 without a valid key.
SWAPI_API_KEY='xxxxxx'
