# Mapping of canonical fediverse software family names to their variants /
# forks as returned by fediverse.observer's `softwarename` field.
#
# When the API receives a softwarename listed here, it is normalized to the
# canonical key. The raw softwarename is preserved in the `software`
# response field. Unmapped softwarenames are returned as-is for both `type`
# and `software`.
#
# Add new forks here when fediverse.observer starts reporting them.
# Run `rake fediverse:check_unknown_software` (if defined) to surface any
# softwarename not covered by this map.

mastodon:
  - mastodon
  - hometown
  - glitch-soc
  - fedibird

pleroma:
  - pleroma
  - akkoma

misskey:
  - misskey
  - firefish
  - calckey
  - sharkey
  - iceshrimp
  - foundkey
  - cherrypick
  - meisskey

lemmy:
  - lemmy
  - mbin
  - kbin

mitra:
  - mitra

pixelfed:
  - pixelfed

peertube:
  - peertube

friendica:
  - friendica

hubzilla:
  - hubzilla

gnusocial:
  - gnusocial
  - statusnet
  - postactiv

gotosocial:
  - gotosocial

funkwhale:
  - funkwhale

bookwyrm:
  - bookwyrm

writefreely:
  - writefreely

plume:
  - plume
