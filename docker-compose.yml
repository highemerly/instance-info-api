services:
  api:
    build: .
    ports:
      - "3001:3000"
    env_file:
      - .env.production
    environment:
      - RAILS_ENV=production
      - RAILS_LOG_TO_STDOUT=true
    volumes:
      - sqlite_data:/app/storage

volumes:
  sqlite_data:
